I used the Supabase ORM to perform relational joins between the users and user_profiles tables.

Learned to manage 1:1 relationships using unique identifiers across tables.
Understood how to use tokens as a secure bridge between the frontend and API.
Practiced using middleware to intercept and validate incoming requests.

The main challenge was resolving a GitHub push block caused by  Personal Access Token accidentally saved in package.json. I resolved this by cleaning the file, using git commit --amend to scrub the Git history, and force-pushing the branch after unblocking the secret in repository settings.
